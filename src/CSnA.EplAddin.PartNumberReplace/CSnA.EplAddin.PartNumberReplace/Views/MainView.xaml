<UserControl x:Class="CSnA.EplAddin.PartNumberReplace.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSnA.EplAddin.PartNumberReplace.Views"
             xmlns:vm="clr-namespace:CSnA.EplAddin.PartNumberReplace.ViewModels"
             xmlns:col="clr-namespace:System.Collections;assembly=mscorlib"
             xmlns:tk="clr-namespace:GeKtvi.Toolkit.WpfKit.Controls;assembly=GeKtvi.Toolkit.WpfKit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <tk:DataGridGK CanUserSortColumns="False" SelectionMode="Extended" SelectionUnit="CellOrRowHeader" Grid.Row="0" ItemsSource="{Binding PartNumbers}">
        </tk:DataGridGK>
        <TextBlock Grid.Row="1" Text="{Binding ErrorMessage, UpdateSourceTrigger=PropertyChanged}" Foreground="Red"/>
        <DockPanel Grid.Row="2" Margin="20"  LastChildFill="False">
            <!--<Button Margin="17, 0, 0, 0" Width="123" Height="26" DockPanel.Dock="Right">Cancel</Button>-->
            <Button Command="{Binding Replace}" Margin="17, 0, 0, 0" Width="123" Height="26" DockPanel.Dock="Right">Заменить</Button>
        </DockPanel>
    </Grid>
</UserControl>
